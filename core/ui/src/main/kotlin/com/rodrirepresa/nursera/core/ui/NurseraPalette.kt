package com.rodrirepresa.nursera.core.ui

import androidx.compose.ui.graphics.Color

/**
 * Neo-brutalist palette shared by the design system.
 *
 * These were previously duplicated as raw hex literals across components,
 * which made restyling error prone.
 */
object NurseraPalette {
    val Ink = Color(0xFF1A1A1A)
    val Paper = Color(0xFFFFF8F0)
    val Yellow = Color(0xFFFFE566)
    val Muted = Color(0xFF8A8A8A)
}
